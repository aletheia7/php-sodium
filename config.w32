// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sodium", "for sodium support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("sodium", "enable sodium support", "no");

if (PHP_SODIUM != "no") {
	EXTENSION("sodium", "sodium.c");
}

